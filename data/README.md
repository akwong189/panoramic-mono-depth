# Data Class Generation

Dataset classes need to be generated to used for training. The way of creating datasets are shown in the `template.py` file which just holds the basics of creating a dataset generator class. Additionally, there should be a file/function to generate the splits and preprocessing for each image.